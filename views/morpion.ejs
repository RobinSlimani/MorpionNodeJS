<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Morpion</title>
</head>
<style>
input {
    margin: 30px;
}
</style>
<body>
    <h1>Jeu de Morpion</h1>
    <form id="morpion-form" method="post" action="/submit">
        <table>
            <% for (let row = 0; row < 3; row++) { %>
                <tr>
                    <% for (let col = 1; col <= 3; col++) { 
                        let index = row * 3 + col; %>
                        <td>
                            <input type="checkbox" id="case<%= index %>" name="<%= index %>" <%= checkboxStates[index] === 'on' ? 'checked' : '' %>>
                            <input type="hidden" name="<%= index %>-hidden" value="<%= checkboxStates[index] || '' %>">
                        </td>
                    <% } %>
                </tr>
            <% } %>
        </table>
        <% if (compteur) { %>
            <div><%= compteur %></div>
        <% } %>
        <button type="submit" id="valider">Valider</button>
    </form>
</body>
</html>
